# Core
Django>=5.2,<7.0
djangorestframework>=3.15,<4.0
djangorestframework-simplejwt>=5.3,<6.0
django-cors-headers>=4.4,<5.0

# Production database driver (optional).
# Uncomment to use PostgreSQL instead of SQLite (see settings.py DATABASES).
# psycopg[binary]>=3.1,<4.0
